<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cooknchill">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".auth.EditProfileActivity"
            android:label="@string/title_activity_edit_profile"></activity>
        <activity android:name=".auth.SignUp2Activity" />
        <activity
            android:name=".auth.LoginActivity"
            android:label="Login" />
        <activity android:name=".auth.HomeActivity" />
        <activity android:name=".auth.SignUpActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>